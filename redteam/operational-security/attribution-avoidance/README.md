# Attribution Avoidance Module

Guidance and automation for rotating personas, infrastructure, and comms profiles to prevent attribution during authorized ops.

Use `persona_manager.py` to generate per-operation personas (email handles, cover companies, VPN egress). Outputs JSON that ties back to audit logs.
